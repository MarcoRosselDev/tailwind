function smallestCommons(arr){
  // si el earray es [1, 5]
  // el mcm de 1 y 5 es 5
  // pero quiere el mcm de todos los numeros entre el 1 y el 5
  // seria algo asi: mcm de 1, 2, 3, 4 y 5.
  // podriamos aplicar matrices, asta que todos sean 1 y luego multiplicar los reciduos.
  // en el caso del [1, 5] seria 2*2*3*5 = 60
  console.log(arr[0], arr[1], suma)
  return suma
}

smallestCommons([1, 5])// should return 60