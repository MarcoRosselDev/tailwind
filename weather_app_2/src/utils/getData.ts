/* export default async function getData(link: string) {
  let response = await fetch(link);
  if (response.ok) {
    let data = await response.json();
    return { ...data };
  } else {
    throw new Error("error en el getData fn");
  }
}
 */
